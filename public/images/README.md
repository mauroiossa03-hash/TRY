Drop a static fallback image here as `hero-fallback.jpg` (referenced by
`HERO_FALLBACK_IMAGE` in `src/lib/config.ts`).

This is shown on mobile, when `prefers-reduced-motion` is set, and as the
video `poster` while `hero-loop.mp4` loads. Use a high-quality still frame
of table tennis in play, ideally matching the video's first frame.
